<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- maison.maison action window -->
    <record id="maison_maison_action" model="ir.actions.act_window">
        <field name="name">Trano amidy</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">maison.maison</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Lany ny trano fa afaka miandry</p>
            
        </field>
    </record>

</odoo>
