<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- maison.maison action window -->
    <record id="pivarotra_maison_action" model="ir.actions.act_window">
        <field name="name">Mpivarotra Maison</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pivarotra.maison</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
        <field name="name" />
            
        </field>
    </record>

</odoo>